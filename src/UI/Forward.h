#pragma once
#include <Ty/Base.h>

namespace UI {

struct Controller;

template <typename T>
struct View;

struct VStack;
struct HStack;
struct TreeGen;

enum class Axis;
enum class Spacing;
enum class Alignment;
enum class Sizing;

struct StackProperties;

struct TreeGen;
struct HTMLTreeGen;
struct DebugTreeGen;

}
